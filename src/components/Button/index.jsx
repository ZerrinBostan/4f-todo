import styled from 'styled-components';

const Button = () => {
    const Button = styled.button``;

    return (
        <Button as="a" href="/">
            Button
        </Button>
    );
};
export default Button;
